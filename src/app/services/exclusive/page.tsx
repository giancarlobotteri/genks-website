import { createExclusive } from "../actions";
export default async function ExclusivePage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const q=await searchParams;return <div className="portal-shell narrow-shell"><span className="eyebrow">EXCLUSIVE LICENSE</span><h1>Make it yours.</h1><p className="muted">Exclusive terms are negotiated directly. Your request is saved before WhatsApp opens.</p>{q.error&&<p className="notice error">Could not save the request. Check every field.</p>}<form action={createExclusive} className="premium-form"><input type="hidden" name="beatId" value={typeof q.beat==="string"?q.beat:""}/><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" type="tel"/></label><label>Message<textarea name="message" rows={5}/></label><button className="button button-primary">SAVE & OPEN WHATSAPP</button></form></div>}
