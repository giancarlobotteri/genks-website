import { createSupabaseAdminClient } from "@/lib/supabase/admin";
export default async function Customers(){const db=createSupabaseAdminClient();const {data}=await db.from("profiles").select("id,display_name,artist_name,phone,segment,marketing_consent,created_at").order("created_at",{ascending:false});return <><header className="admin-heading"><h1>Customers</h1></header><div className="admin-table">{data?.map(x=><div className="admin-table-row" key={x.id}><span><strong>{x.artist_name||x.display_name||"Customer"}</strong><small>{x.phone}</small></span><span>{x.segment}</span><span>{x.marketing_consent?"Marketing opt-in":"Transactional only"}</span></div>)}</div></>}
